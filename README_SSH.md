# 🔐 Настройка SSH для деплоя

## У вас есть SSH ключ с паролем

Если при подключении просит passphrase для ключа `/Users/glebstudent/.ssh/id_ed25519`, это нормально.

## Варианты деплоя:

### 1. Использовать скрипт (он сам спросит пароль)

```bash
cd /Users/glebstudent/collectx/collectx-cards
./deploy-simple.sh
```

Когда скрипт попросит подключиться, просто введи passphrase для SSH ключа.

---

### 2. Подключиться вручную сначала

```bash
# Подключись к серверу (введи passphrase когда спросит)
ssh root@31.130.155.210
# или попробуй другого пользователя, если root не работает
```

Если подключение работает, то можешь использовать скрипт.

---

### 3. Использовать пароль вместо ключа

Если SSH ключ не работает, можно использовать пароль:

```bash
# Добавь опцию для использования пароля
ssh -o PreferredAuthentications=password -o PubkeyAuthentication=no root@31.130.155.210
```

---

### 4. Добавить SSH ключ в ssh-agent

Чтобы не вводить passphrase каждый раз:

```bash
# Запусти ssh-agent
eval "$(ssh-agent -s)"

# Добавь ключ (спросит passphrase один раз)
ssh-add ~/.ssh/id_ed25519
```

Теперь можешь запускать скрипт без ввода passphrase.

---

## Проверка подключения

Попробуй подключиться:

```bash
# С passphrase (введешь когда спросит)
ssh root@31.130.155.210 "echo 'Connected!'"

# Или попробуй другого пользователя (ubuntu, admin, user и т.д.)
ssh ubuntu@31.130.155.210
ssh admin@31.130.155.210
```

---

## Какой пользователь?

Обычно на серверах используют:
- `root` - суперпользователь
- `ubuntu` - на Ubuntu серверах
- `admin` - на некоторых облачных платформах
- `deploy` - для деплоя

Попробуй каждый, чтобы найти правильный.

---

## Быстрый старт

1. **Проверь подключение:**
   ```bash
   ssh root@31.130.155.210
   # или
   ssh ubuntu@31.130.155.210
   ```

2. **Если подключился, запусти скрипт:**
   ```bash
   cd /Users/glebstudent/collectx/collectx-cards
   ./deploy-simple.sh
   ```

3. **Следуй инструкциям скрипта**

---

**Готов начать? Попробуй подключиться к серверу! 🚀**

