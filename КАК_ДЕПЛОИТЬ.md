# 🚀 Как задеплоить на сервер

## ❓ Вопрос: Нужно ли что-то менять на GitHub?

**Короткий ответ: НЕТ, не обязательно!**

Есть два варианта:

---

## ⚡ Вариант 1: Без GitHub (быстро и просто)

**Не нужно ничего пушить на GitHub!**

Просто скопируем файлы напрямую на сервер.

### Как делать:

```bash
cd /Users/glebstudent/collectx/collectx-cards
./deploy-simple.sh
```

Всё! Файлы скопируются напрямую, без GitHub.

**Плюсы:**
- ✅ Быстро
- ✅ Не нужно коммитить
- ✅ Работает сразу

---

## 🌟 Вариант 2: Через GitHub (рекомендуется для будущего)

Сначала сохранить на GitHub, потом клонировать на сервере.

### Как делать:

```bash
cd /Users/glebstudent/collectx/collectx-cards

# 1. Закоммитить и запушить изменения
git add .
git commit -m "Add deployment and cards system"
git push origin feature-wallet

# 2. Деплой через GitHub
./deploy-via-git.sh
```

**Плюсы:**
- ✅ Всё сохранено на GitHub
- ✅ Легко обновлять (git pull)
- ✅ Можно откатиться

---

## 🎯 Что выбрать?

### Для первого раза (быстро протестировать):
👉 **Вариант 1** - `./deploy-simple.sh`

### Для постоянной работы:
👉 **Вариант 2** - сначала git push, потом `./deploy-via-git.sh`

---

## 💡 Моя рекомендация:

**Шаг 1:** Задеплой быстро через вариант 1:
```bash
./deploy-simple.sh
```

**Шаг 2:** После того как всё заработает - сохрани на GitHub:
```bash
git add .
git commit -m "Add deployment configs"
git push origin feature-wallet
```

**Шаг 3:** В будущем обновляй через вариант 2 (git pull на сервере).

---

## 🚀 Начинай сейчас:

Просто запусти:

```bash
cd /Users/glebstudent/collectx/collectx-cards
./deploy-simple.sh
```

И следуй инструкциям! Ничего на GitHub менять не нужно. 🎉

---

**Подробнее смотри: `DEPLOY_OPTIONS.md`**

