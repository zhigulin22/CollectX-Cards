# 📦 Варианты деплоя на сервер

## У тебя есть репозиторий на GitHub:
- **Репозиторий:** https://github.com/zhigulin22/CollectX-Cards.git
- **Ветка:** feature-wallet

## 🎯 Два варианта деплоя:

### Вариант 1: Без GitHub (быстро, напрямую) ⚡

**Не нужно ничего пушить в GitHub!**

Просто копируем файлы напрямую на сервер через `rsync` или скрипт.

**Плюсы:**
- ✅ Быстро
- ✅ Не нужно коммитить изменения
- ✅ Работает сразу

**Минусы:**
- ❌ Не сохраняется история на GitHub
- ❌ Сложнее обновлять в будущем

**Как делать:**
```bash
./deploy-simple.sh
```
Скрипт скопирует файлы напрямую на сервер.

---

### Вариант 2: Через GitHub (рекомендуется) 🌟

Сначала сохраняем всё на GitHub, потом клонируем на сервере.

**Плюсы:**
- ✅ Всё сохранено на GitHub
- ✅ Легко обновлять (git pull)
- ✅ Есть история изменений
- ✅ Можно откатиться к старой версии

**Минусы:**
- ❌ Нужно сначала закоммитить и запушить

---

## 📋 Что нужно сделать для варианта 2:

### Шаг 1: Сохранить изменения на GitHub

```bash
cd /Users/glebstudent/collectx/collectx-cards

# Посмотреть что изменилось
git status

# Добавить все новые файлы
git add .

# Закоммитить
git commit -m "Add deployment configs and card collection features"

# Запушить на GitHub
git push origin feature-wallet
```

### Шаг 2: Деплой через GitHub

После этого на сервере можно будет клонировать и обновлять через git:

```bash
# На сервере
cd /var/www
git clone -b feature-wallet https://github.com/zhigulin22/CollectX-Cards.git collectx
cd collectx
# Настройка и запуск...
```

---

## 🎯 Какой вариант выбрать?

### Выбирай вариант 1 (без GitHub), если:
- ✅ Нужно быстро задеплоить
- ✅ Не хочешь сейчас коммитить изменения
- ✅ Это тестовый деплой

### Выбирай вариант 2 (через GitHub), если:
- ✅ Хочешь сохранить всё на GitHub
- ✅ Планируешь обновлять проект регулярно
- ✅ Работаешь в команде

---

## 💡 Моя рекомендация:

**Для первого деплоя:**
1. Используй **вариант 1** (быстро, напрямую) - чтобы быстро всё задеплоить и проверить
2. После того как всё заработает - **закоммить и запушить** изменения на GitHub
3. В будущем обновлять через **вариант 2** (git pull)

Так ты быстро задеплоишь, но потом всё будет на GitHub для обновлений.

---

## 🚀 Быстрый старт (вариант 1 - без GitHub):

```bash
cd /Users/glebstudent/collectx/collectx-cards
./deploy-simple.sh
```

Готово! Файлы скопируются напрямую на сервер.

---

## 📝 После успешного деплоя (сохранить на GitHub):

Когда всё заработает, сохрани изменения:

```bash
cd /Users/glebstudent/collectx/collectx-cards

# Добавить всё
git add .

# Закоммитить
git commit -m "Add deployment configuration and card collection system"

# Запушить
git push origin feature-wallet
```

---

**Какой вариант выбираешь? Просто запусти `./deploy-simple.sh` для быстрого деплоя! 🚀**

